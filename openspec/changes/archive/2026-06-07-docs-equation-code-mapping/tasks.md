## 1. Ground the mapping in shipped code

- [x] 1.1 Re-confirm every function named in each doc's "Equation → implementation map" exists and is quotable, and pick the signature + key arithmetic line(s) per equation — ReSTIR: `reservoirUpdate/Merge/Finalize` (`restir/reservoir.slang`), `restirEvalRef`/`_mixPdf`/`restirFillReservoir`/`restirResolveReservoir`/`restirDirectional` (`restir/light_ris.slang`), `restirFill`/`restirSpatial`/`restirResolve`/`restirLoadLane` (`restir/restir_primary.slang`), `reuseDirect` (`sampling/reuse.slang`)
- [x] 1.2 Same for Neural: `neuralCondition`/`sampleNeural`/`pdfNeural` (`sampling/neural_proposal.slang`), `nf_flow_forward/inverse`/`nf_mlp`/`nf_linear`/`nf_decode`/`nf_rqs_fwd`/`nf_rqs_inv`/`nf_square_to_hemi`/`nf_hemi_to_square` (`sampling/neural_flow.slang`), `proposalWeights`/`sampleBounceDirection`/`mixtureProposalPdf` (`sampling/proposal.slang`), record path (`integrators/path_record.slang`)
- [x] 1.3 Decide the `key` per equation and which need multi-slice (`sig`+`core`) vs single-slice

## 2. Generator (docs/diagrams/embed_code.cjs)

- [x] 2.1 Parse `// DOC:<key>#<slice> start/end` regions from a configured set of `.slang` files into a `{key#slice: lines}` map; detect unbalanced markers
- [x] 2.2 Parse `<!-- CODE:<key> s1,s2 --> … <!-- /CODE:<key> -->` regions in the target docs; for each, emit a ```slang block = provenance `// from <file>.slang` + the named slices in order, inserting an elision line (`    // …`) between source-non-adjacent slices
- [x] 2.3 Idempotent in-place rewrite of `docs/ReSTIR.md` and `docs/NeuralGuiding.md`
- [x] 2.4 `--check` mode: non-zero exit on an unresolved `CODE:` key, empty slice list, or unbalanced `DOC:` marker; warn (not fail) on a `DOC:` marker no doc references
- [x] 2.5 Short usage note (header comment / `docs/diagrams/README`) on how to run and regenerate

## 3. Annotate shaders with `// DOC:` markers

- [x] 3.1 ReSTIR: add `start/end` comment markers around the chosen signature + key-line slices in `restir/reservoir.slang`, `restir/light_ris.slang`, `restir/restir_primary.slang`, `sampling/reuse.slang` (comments only — no semantic change, no `.spv` recompile)
- [x] 3.2 Neural: same in `sampling/neural_flow.slang`, `sampling/neural_proposal.slang`, `sampling/proposal.slang`, `integrators/path_record.slang`

## 4. Wire docs

- [x] 4.1 `docs/ReSTIR.md`: under each equation §1–§7, add the `<!-- CODE:key … -->` placeholder and the hand-written per-equation symbol→code table; keep the existing `> Implements:` pointer and global map
- [x] 4.2 `docs/NeuralGuiding.md`: same for §1–§11 (symbol tables carry the real weight here: `θ/s/h_k/w_k/d_k/y_k → theta/delta/hgt/w/d0/y0`)
- [x] 4.3 Run `embed_code.cjs` to fill all placeholders; commit the generated blocks

## 5. Docs upkeep + verify

- [x] 5.1 Note in `CLAUDE.md` documentation-upkeep section that `docs/ReSTIR.md` + `docs/NeuralGuiding.md` carry generated code blocks regenerated by `embed_code.cjs --` and checked by `--check`
- [x] 5.2 Run `embed_code.cjs --check` clean; eyeball each generated snippet against its equation and symbol table for transcription/indent sanity
- [x] 5.3 `openspec validate docs-equation-code-mapping --strict`
