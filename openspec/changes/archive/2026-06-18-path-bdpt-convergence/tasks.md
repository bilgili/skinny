## 1. Megakernel path fix

- [x] 1.1 Add a `glass_arealight` headless convergence test that renders the path tracer and asserts it diverges from the pbrt reference today (FLIP ≈ 0.058, caustic/reflection absent) — captures the bug before the fix. (Reference re-anchored from BDPT to the pbrt EXR during apply — BDPT measured ~1.7× too bright vs pbrt, see design D4.)
- [x] 1.2 In `shaders/integrators/path.slang`, carry `spawnedBySpecular` (false for the primary ray; set from `bsdfSample.pdf <= 0.0` after each bounce) and gate the emissive-triangle emission on it: `if (bounce == 0 || numEmissiveTriangles == 0 || spawnedBySpecular)`
- [x] 1.3 Apply the identical change at `shaders/integrators/path_record.slang:89` (the neural-record dump variant)
- [x] 1.4 `main_pass.spv` regenerated by the in-process runtime compile (`vk_compute._compile_slang` writes `shaders/main_pass.spv`); standalone `slangc` can't build it because the skin path imports a per-scene-generated `skinny_skin_layered_bsdf_genslang.slang` the runtime codegens first (pre-existing). Megakernel convergence verified path ≈ pbrt (glass FLIP 0.025) on BOTH Vulkan AND native Metal (identical relMSE/FLIP — fix is backend-agnostic).

## 2. Convergence regression test

- [x] 2.1 Promote 1.1 into a passing convergence test: relMSE/FLIP vs the pbrt reference below tolerance on `glass_arealight`, env/default-light disabled, read from `read_accumulation_hdr` (`tests/pbrt/test_convergence.py::test_glass_arealight_path_converges_to_pbrt`)
- [x] 2.2 Add a non-specular regression: a purely diffuse scene matches the pbrt reference within tolerance — proves the change only fires on delta bounces (`test_diffuse_scene_unchanged_and_unbiased`)
- [x] 2.3 Energy-conservation / no-double-count check: folded into the diffuse-unbiased test — a double-count on non-delta surfaces would push the diffuse image bright (away from pbrt) and raise FLIP/relMSE past tolerance. No furnace scene exists in the pbrt corpus; the diffuse vs-pbrt gate is the equivalent no-spurious-gain assertion.

## 3. Wavefront path fix

- [x] 3.1 Located at `shaders/wavefront/wf_shade_common.slang:89-90` (`wfFinishShade`, the single emission site, called from both material paths in `wavefront_path.slang`). Confirmed same bias: pre-fix wavefront glass mean 0.147 / FLIP 0.061 (identical to megakernel).
- [x] 3.2 Applied the delta-gated rule via the existing-but-unused `PATH_FLAG_SPECULAR` path-state bit: set on spawn when `bsdfSample.pdf <= 0.0` (cleared otherwise), read at the emission gate as `pathSpecular(s)`. Carries across kernels in `WavefrontPathState.flags`.
- [x] 3.3 Added `test_glass_arealight_wavefront_converges_to_pbrt` (`--execution-mode wavefront`, 256 spp): post-fix FLIP 0.032 / relMSE 0.031 vs pbrt (was 0.061/0.061). Passes on native Metal.

## 4. Documentation and close-out

- [x] 4.1 Documented the specular→area-light MIS rule: `docs/Megakernel.md` §3.1 (BSDF-hit vs NEE + the delta rule) and `docs/Wavefront.md` (PATH_FLAG_SPECULAR carries the rule across kernels)
- [x] 4.2 Updated `CHANGELOG.md` (Unreleased › Fixed); noted the runtime-regenerated `main_pass.spv` + the pbrt-reference re-anchor + the BDPT over-brightness follow-up
- [x] 4.3 Close-out GREEN: `ruff` clean (changed files); convergence gate 3/3 GPU pass on native Metal; pbrt corpus parity gate 3/3 GPU pass (no regression from the backend-resolution change); import-smoke 3/3; `openspec validate path-bdpt-convergence --strict` → valid; `embed_code.cjs --check` → docs in sync.
