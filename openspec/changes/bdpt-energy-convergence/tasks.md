## 1. BDPT emissive-hit MIS gate

- [x] 1.1 Reproduce the bug headless on `diffuse_arealight` (no delta transport):
      `mean(bdpt)/mean(ref)` = **1.756**, raw relMSE 0.346, exposure-aligned relMSE
      0.026 (the alignment hides it — see design D4). Saved a labelled
      ref·path·bdpt side-by-side at the shared tonemap.
- [x] 1.2 In `shaders/integrators/bdpt.slang`, gate the `(s ≥ 2, t = 0)` emissive
      eye-vertex loop: add `z.throughput * z.emission` at full weight only when
      `s == 2 || fc.numEmissiveTriangles == 0u || eye[s - 2].isDelta` — the BDPT
      translation of `path.slang`'s `bounce == 0 || numEmissiveTriangles == 0 ||
      spawnedBySpecular`. Otherwise `connectT1` (the t = 1 NEE) owns the area light.
- [x] 1.3 Apply the identical gate at `shaders/wavefront/wavefront_bdpt.slang`
      (`wfBdptConnectLane`, shared by the NEE and FULL connect slots).
- [x] 1.4 `main_pass.spv` regenerated by the in-process runtime compile
      (`vk_compute._compile_slang`); standalone `slangc` can't build it (the skin
      path imports the per-scene-generated `skinny_skin_layered_bsdf_genslang.slang`
      the runtime codegens first — pre-existing). `main_pass.spv` is gitignored in
      this worktree (runtime artifact), so nothing to commit there.

## 2. BDPT absolute-energy gate

- [x] 2.1 Add `tests/pbrt/test_bdpt_energy.py`: compares **un-aligned** mean energy
      (the parity/convergence gates exposure-align and cannot see a uniform scale).
      Two assertions per case — `|mean(bdpt)/mean(ref) - 1| <= 0.20` and
      `|mean(bdpt)/mean(path) - 1| <= 0.08`.
- [x] 2.2 Cases: `diffuse_arealight` megakernel (the clean no-delta signal),
      `glass_arealight` megakernel (specular present), `diffuse_arealight` wavefront
      (covers the wavefront shader fix). All `@pytest.mark.gpu`.
- [x] 2.3 Pre-fix the gate fails decisively (1.756 vs tol 0.20; bdpt/path 2.02 vs
      tol 0.08); post-fix 3/3 pass on native Metal in 15 s.

## 3. Verification and close-out

- [x] 3.1 Post-fix energy: diffuse mega ×0.876, diffuse wavefront ×0.875, glass mega
      ×0.874 — BDPT now tracks the path tracer (bdpt/path ≈ 1.00–1.01). Verified on
      native Metal AND Vulkan/MoltenVK (diffuse mega ×0.876 on both — backend-agnostic).
      Saved post-fix ref·path·bdpt side-by-sides for both scenes.
- [x] 3.2 No regression: existing GPU gates green — `test_convergence.py` 3/3
      (path tracer unchanged), `test_parity.py` corpus gate 3/3 (relMSE/FLIP still
      within manifest tol). Path tracer numbers unchanged (it was not touched).
- [x] 3.3 Docs: `docs/Megakernel.md` (BDPT t=0 emissive-hit MIS gate),
      `docs/Wavefront.md` (the gate in `wfBdptConnectLane`), `CHANGELOG.md`
      (Unreleased › Fixed).
- [x] 3.4 `ruff check` clean on changed files; `openspec validate
      bdpt-energy-convergence --strict` → valid.
